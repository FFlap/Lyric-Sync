# Core
flask>=3.0
yt-dlp>=2024.1

# Alignment (CPU)
torch>=2.0
torchaudio>=2.0,<2.9  # 2.9 removes functional.forced_align used by align_words.py
audio-separator==0.44.2
faster-whisper>=1.0
librosa>=0.10
soundfile>=0.12
numpy>=1.24

# System: ffmpeg required (see README for install per OS)
